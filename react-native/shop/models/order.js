class Order {
    constructor(id, items, totalAmount, date){
        this.id = id,
        this.items = items,
        this.totalAmoun = totalAmount,
        this.date = date;
    };
};

export default Order;